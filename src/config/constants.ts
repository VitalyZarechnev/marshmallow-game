// Shared game constants (non-difficulty-dependent)

// Water & environment
export const WATER_Y = 1.5;
export const SEABED_Y = -3;
export const ISLAND_HEIGHT = 2;

// Maze grid config
export const GRID_COLS = 15;
export const GRID_ROWS = 15;
export const ISLAND_W = 4.5;
export const ISLAND_D = 4.5;
export const SPAWN_COL = Math.floor(GRID_COLS / 2);
export const SPAWN_ROW = Math.floor(GRID_ROWS / 2);

// Sand island
export const SAND_PARTICLE_COUNT = 40;
export const WATER_BOTTOM = -4;
